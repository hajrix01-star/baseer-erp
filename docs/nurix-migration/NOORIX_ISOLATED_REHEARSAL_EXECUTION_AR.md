# سجل تنفيذ تجربة الاستعادة المعزولة لنوركس

**تاريخ التنفيذ:** 2026-08-27  
**الحالة:** مكتمل بنجاح — للاكتشاف والتحليل فقط، وليس ترحيلاً إلى بصير أو إطلاقاً إنتاجياً.

## نطاق التنفيذ

- المصدر: `noorix-system-archive-131-cmtb6iw6r0009an2w5t165qm3.tar.gz`.
- بصمة SHA-256: `D8836B78AB52B8C297D87115767314E0560A2C09993000C0C503E5BA27C7B9AD`.
- تم الاستعادة إلى قاعدة مستقلة باسم `nurix_rehearsal` داخل حاوية PostgreSQL 16 مستقلة.
- لم يُنشأ اتصال ببصير، ولم تُستخدم أي بيانات اعتماد لبصير أو للخادم، ولم تُنشر منافذ.

## ضوابط العزل والحماية المثبتة

- الحاوية تعمل بوضع شبكة `none` ولا تملك أي ربط منافذ.
- ملف المصدر مركّب للقراءة فقط (`/input/noorix.tar.gz`).
- مساحة بيانات PostgreSQL حجم Docker مستقل خاص بالتجربة.
- بعد الاستعادة فُعلت القيمة الافتراضية `default_transaction_read_only = on` للقاعدة.
- أُنشئ حساب `nurix_analyst` للتحليل فقط: يملك `SELECT` على جداول `public` ولا يملك `INSERT` أو `DELETE`.

## تحقق النسخة والاستعادة

- اجتاز ملف PostgreSQL المخصص فهرسة `pg_restore` قبل الاستعادة.
- الفحص البنيوي لم يظهر `COPY PROGRAM` أو `ALTER SYSTEM` أو امتدادات غير متوقعة؛ توجد فقط دوال نوركس الخاصة بوحدة `orders_v4`.
- تمت الاستعادة باستخدام `pg_restore --exit-on-error --no-owner --no-privileges`.
- اكتملت بلا أخطاء.
- عدد جداول `public`: **73**.
- قيود العلاقات الخارجية: **166**؛ القيود غير الموثقة: **0**.
- ترحيلات Prisma المسجلة: **258**.

## جرد أولي غير حساس

| المجال | الجداول/السجلات |
| --- | ---: |
| الشركات | 8 شركات |
| المستأجرون | 1 |
| المستخدمون | 11 |
| الموظفون | 61 |
| الحسابات | 196 |
| الموردون | 457 |
| الخزن | 29 |
| الفواتير | 3,782 |
| قيود الأستاذ | 5,280 |
| مسيرات الرواتب | 21 |
| سجلات التدقيق | 4,808 |
| مهام النسخ في نوركس | 116 |
| `orders_v4` (العناصر/الوثائق/السطور) | 0 / 0 / 0 |

## نتيجة اللجنة

التجربة **صالحة للاكتشاف وبناء خريطة ترحيل** فقط. لا تمثل موافقة على استيراد تلقائي أو انتقال إنتاجي إلى بصير.

يبقى التحفظ المهم: الأرشيف لا يحتوي ملفات المرفقات الفعلية، بل مجلدات رفع فارغة؛ لذلك لا يمكن اعتباره استعادة كاملة للملفات والوثائق حتى تُسلَّم ملفات الرفع مع قائمة محتويات وبصمة تحقق.

## المرحلة التالية المعتمدة

1. استخراج قاموس الحقول والعلاقات لكل شركة من قاعدة `nurix_rehearsal` بحساب التحليل فقط.
2. اعتماد ما يدخل إلى بصير وما يُؤرشف، بما في ذلك معالجة المرفقات الغائبة.
3. إنشاء `baseer-migration-staging` فارغة ومنفصلة.
4. تنفيذ ترحيل تجريبي قابل للإعادة مع عدادات وتقرير مطابقة لكل شركة.
5. لا يتم أي ترحيل نهائي إلا بعد توقيع تقرير المطابقة وموافقة صريحة.
