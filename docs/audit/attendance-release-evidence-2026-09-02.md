# دليل إصدار الحضور والانصراف — 2026-09-02

## هوية المرشح

- فرع المصدر: `codex/attendance-release-candidate`.
- مرشح الـruntime: `7cbddbf17aad71b22a23e63db817fe0b8878d640`.
- التعديل السابق المضمّن في المرشح: `9d1c3f67e41e7fe8b592204915bff80ed73843d7`.
- النطاق: حضور وانصراف متعدد الشركات، موقع اختياري، اتفاق ساعات الموظف، إغلاق المدير الموثق، والاحتفاظ بالموقع 14 يوماً.

## سلسلة الـartifact المحلية

تم البناء محلياً من المرشح أعلاه فقط، من دون push أو نشر:

| الصورة | الوسم المحلي | digest المحلي |
|---|---|---|
| API | `baseer-erp-api:attendance-7cbddbf` | `sha256:82bc30f2005c7b24fc5fa215d2bf1f7864d69048952b6ccf97e4793bf90e232f` |
| migrate | `baseer-erp-migrate:attendance-7cbddbf` | `sha256:07778026661a262167f25d60ea53973d79f1d39cb438a721d0878136c04a9f34` |
| web | `baseer-erp-web:attendance-7cbddbf` | `sha256:783a8c0fcedd2d12f681f4c3ef7197fec021412c9d6807695daf063640338ee9` |

تم أيضاً إصلاح `Dockerfile.web`: يبني الآن حزمة العقود قبل الواجهة في حاوية نظيفة. كان البناء السابق يفشل بسبب غياب `@baseer-erp/contracts` داخل الحاوية؛ أعيد بناء صورة web بعد الإصلاح بنجاح.

## دليل الاستعادة المعزول

- المصدر: `baseer_erp_test` المحلية فقط.
- الوجهة الجديدة: `baseer_erp_attendance_restore_verify_20260902`.
- SHA-256 لملف النسخة الاحتياطية: `e3df1361f0a73c8a224b9cd847e57e18bb73560dc6fc04df8ab0558f467b7bb1`.
- بعد الاستعادة: 148 ترحيلاً فعالاً، 0 ترحيل فاشل.
- smoke: عمود `Company.attendanceLocationEnabled` ودالة حماية/تنقيح `prevent_attendance_event_mutation` موجودان في القاعدة المستعادة.

هذه العملية لم تتصل بقاعدة التشغيل `baseer_erp` ولم تعدّلها.

## التحقق المرتبط بالمرشح

- `node scripts/run-attendance-http-verification.mjs`: ناجح.
  - يتحقق من حدود المالك/المدير، الإغلاق الإداري بوقت وسبب، idempotency، الموقع off/on، تنقيح الإحداثيات بعد 14 يوماً، وعدم قابلية تغيير الحدث، و12 تسجيل حضور متزامناً بلا `429` أو `500`.
- `npm run check --workspace @baseer-erp/web`: ناجح.
- `git show --check 7cbddbf…`: ناجح.

## الرجوع

لا يوجد rollback مخطط مباشر بعد دخول بيانات `ADMIN_CHECK_OUT` أو تنقيح الموقع إلى `NULL`. الرجوع الآمن هو:

1. إيقاف نشر المرشح واستخدام صورة runtime سابقة معروفة.
2. معالجة المخطط بتصحيح أمامي متوافق، أو استعادة نسخة قاعدة معتمدة عند الحاجة التشغيلية.
3. عدم حذف أحداث الحضور أو إعادة إنشاء الإحداثيات المنقحة.

## القرار

**CONDITIONAL GO لمرحلة النشر المنضبط فقط، وليس نشرًا منفذاً.** لا توجد P0/P1 وظيفية مثبتة في المرشح أو الترحيل أو الاستعادة المعزولة. قبل النشر الفعلي يجب نقل الصور نفسها إلى سجل/مضيف الإصدار المعتمد بمرجع immutable، وتعيين بيئة `private-online` الحقيقية وخطة الرجوع، ثم تنفيذ migrate وhealth check في نافذة التشغيل.

الملاحظة المتبقية P2: يفضّل إضافة اختبار قبول صريح لعبور منتصف الليل و`WorkTerms`/الراحة الأسبوعية إلى باقة HTTP التالية؛ منطقها موجود ومراجع، لكنها ليست ضمن سيناريو HTTP أعلاه.
