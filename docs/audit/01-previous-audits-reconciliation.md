# المرحلة 1 — مطابقة الفحوصات السابقة مع الكود الحالي

## النتيجة

**مكتملة مع ملاحظات.** استُخدمت وثائق الحوكمة الحالية باعتبارها سلطة الحالة، ثم طوبقت الملاحظات الجوهرية مع الشجرة الحالية. لا تُعامل نجاحات commits سابقة كدليل على التغييرات غير الملتزم بها الحالية.

## مصادر الأدلة السابقة

- السلطة الحالية: `docs/governance/CURRENT_DELIVERY_AUTHORITY.md`.
- سجل الوحدات: `docs/governance/MODULE_DELIVERY_REGISTER.md`.
- سجل الجودة: `docs/governance/QUALITY_EVIDENCE_AND_COMMITTEE_REGISTER.md`.
- التسليم: `BASEER_ERP_HANDOFF.md` و`README.md`.
- تدقيق الصلاحيات: `docs/governance/PERMISSION_SURFACE_AUDIT_2026-08-24.md`.
- قبول مخاطرة Prisma: `docs/governance/PRISMA_PRIVATE_DEPLOYMENT_RISK_ACCEPTANCE_2026-08-22.md`.
- تدقيقات 2026-08-25: navigation، report snapshots، وسياسة hybrid reporting.
- أثر الاختبار المحلي الوحيد الحالي: `test-results/.last-run.json`.
- جرد TODO/FIXME/HACK الدقيق في الكود الحالي.

## سجل المطابقة

| الملاحظة السابقة | التصنيف الحالي | الدليل الحالي | الأثر في هذا التدقيق |
|---|---|---|---|
| النظام ليس جاهزاً للإنتاج العام | **ما زالت قائمة** | `README.md:5-11` و`CURRENT_DELIVERY_AUTHORITY.md` يفرقان صراحة بين الإغلاق المحلي والجاهزية العامة | تُقيّم بوابة الإطلاق مستقلة عن قبول الموديولات المحلي. |
| النسخ الاحتياطي والاستعادة للإنتاج مفتوحان | **ما زالت قائمة جزئيًا** | توجد runbooks تحت `docs/operations/` وشهادة محلية مهملة من Git في `.rehearsal/gate-c/receipt-20260815-022842.json` نجحت على 12 جدولًا؛ المخطط الحالي 149 model ولا يوجد restore حديث/مزود إنتاج/RPO/RTO مثبت | الخبرة المحلية مفيدة، لكنها لا تغلق بوابة backup/restore الحالية. |
| لا benchmark كبير أو EXPLAIN/p95 | **ما زالت قائمة** | `BASEER_ERP_HANDOFF.md:25-31`؛ `QUALITY_EVIDENCE_AND_COMMITTEE_REGISTER.md:204-220` | لا ادعاء قابلية حمل إنتاجي أو بيانات متعددة السنوات. |
| لا Noorix import/dry-run/reconciliation/cutover | **ما زالت قائمة** | `BASEER_ERP_HANDOFF.md:35-47`؛ لا مسار نشر/ترحيل إنتاجي في `ops/` | خارج نطاق الإطلاق إن كان الإصدار greenfield، ومانع إن كان يتطلب بيانات Noorix. |
| تنبيه Prisma/`deepmerge-ts` في build/migrate | **ما زالت قائمة مع تخفيف** | `CURRENT_DELIVERY_AUTHORITY.md` وقرار قبول المخاطرة؛ `Dockerfile` يستبعد الحزم من runtime ويعزل migrate | لا يصنف كتعرض runtime مؤكد، لكنه شرط بوابة خاص بنمط private deployment ويحتاج audit حالي. |
| السماح الافتراضي لصفحات بلا قاعدة صلاحية | **تم إصلاحها من الكود** | `apps/web/src/module-access.ts:16-26` يمنع القاعدة المفقودة؛ `page-registry.ts:11-19` يجعل الحقل إلزامياً | تُبقي اختبارات الرابط المباشر وAPI لازمة؛ إخفاء UI ليس سلطة أمنية. |
| فصل مراجع مدخل المشتريات وإصدار السلفة وأزرار HR | **تم إصلاحها مع دليل سابق** | endpoints الحالية في `purchase-expense.controller.ts:15` و`hr.controller.ts:277-279`؛ اختبارات محدودة في `finance-mocked-auth.spec.ts:93-209` و`hr-mocked-auth.spec.ts:235-241` | سيعاد type check/حراس الصلاحية؛ DB/E2E الكامل يظل منفصلاً. |
| انتهاء `ReportRun` يعيد 404 عاماً وتجديد المستندات ناقص | **تم إصلاحها في الشجرة الحالية، تحتاج تحقق** | `report-snapshot-retry.ts:16-31` يجدد فقط `REPORT_RUN_EXPIRED` مرة؛ `report-document-actions.tsx:20-45`؛ كود API الحالي يميز الرمز | التغييرات غير ملتزم بها؛ لا تغلق قبل type check/اختبارات مناسبة. |
| الشاشة المالية العادية تنشئ snapshot بدل قراءة حية | **تم إصلاحها في الشجرة الحالية، تحتاج تحقق** | `reports-workspace.tsx:48-83` يستخدم `mode="live"` وينشئ official run عند evidence/action؛ خدمات التقارير تستخدم `currentLedgerRevision` للقراءة الحية | يتطابق مع سياسة hybrid، لكن التغييرات الحالية لم يثبتها CI بعد. |
| صفحة `Hajri Tax` ظاهرة بلا مساحة عرض | **ما زالت قائمة** | `page-registry.ts:79` يجعلها visible؛ `App.tsx:280` يعالج تقارير sections 0,1,2,4 ولا يعالج 3 | خلل UX مؤكد وقد يعرض تدفقاً غير مكتمل؛ يجب الإخفاء أو التنفيذ قبل الإطلاق. |
| فجوات صلاحيات التبويبات في المشتريات/HR | **تم إصلاح الجزء الموثق؛ الباقي يحتاج تحقق** | تحديثات نهاية تقرير الصلاحيات `PERMISSION_SURFACE_AUDIT_2026-08-24.md:101-131` والكود الحالي | لا يُكرر كخطر عام؛ سيقتصر سجل المخاطر على فجوات باقية مثبتة. |
| عدم تزامن Marketing contracts وPrisma الذي منع API check | **غير قابلة للتحقق بعد في هذه المرحلة** | الملاحظة السابقة في `PERMISSION_SURFACE_AUDIT_2026-08-24.md:114`؛ المخطط والعميل المولد الحاليان يحتويان كائنات Marketing | يحسمه type check الحالي في المرحلة 8. |
| نجاح Playwright التاريخي `109+1` | **غير قابل للنقل إلى الشجرة الحالية** | السجل التاريخي في `QUALITY...:68`، و`test-results/.last-run.json` الحالي يقول `status: passed` بلا SHA أو عدد حالات أو تقرير مرفق | الإشارة المحلية إيجابية، لكنها لا تكفي كشهادة E2E للمرشح؛ يلزم CI نظيف. |
| قبول الوحدات الأساسية محلياً | **ما زال مرجع النطاق، لا دليل إصدار حالي** | `CURRENT_DELIVERY_AUTHORITY.md` و`QUALITY...:59-66` | يمنع إعادة تصنيف النطاق المقصود كـTODO، لكنه لا يعفي التغييرات اللاحقة من إعادة الاختبار. |

## TODO/FIXME/HACK والحالات المقصودة

البحث الدقيق لم يجد TODO/FIXME/HACK صريحة في مسارات المصدر المستهدفة. النتائج المهمة كانت:

- `packages/output-platform/src/output-platform.ts:38`: رفض صريح لصيغة مخرجات غير مدعومة؛ guard وليس TODO.
- `apps/api/src/ai-platform/ai-skills.ts:189-204`: حالة `NOT_IMPLEMENTED` معلنة لمهارات بلا بوابة runtime موحدة؛ الواجهة تعرضها صراحة (`administration-ai-settings-panel.tsx:436`) والاختبار يتأكد أن المسار القديم لا يبدو جاهزاً. تصنف **غير مكتملة ومقصودة خارج النطاق** ما دامت لا تُعرض كـREADY ولا تُفعّل.
- skip واحد في `shell-accessibility.spec.ts:22` مشروط بالمشروع المحمول، ومتسق مع سجل الجودة التاريخي؛ يحتاج تأكيد عند تشغيل Playwright لا افتراض نجاح.

## عدم التكرار

- ستظهر فجوات backup/restore وbenchmark وNoorix والـPrisma advisory مرة واحدة فقط في سجل المخاطر، مع روابط من تقارير النطاقات المختلفة.
- عطل Hajri Tax يُسجل كخطر واحد يجمع «صفحة ظاهرة» و«مساحة عرض مفقودة».
- مشاكل snapshot القديمة لا تُعاد كمخاطر إن نجحت فحوص الشجرة الحالية؛ إن فشلت، يسجل العطل الحالي فقط.

## فشل أداة المرحلة

أمر `rg` استُخدم مرة بصيغة brace expansion غير مدعومة في PowerShell فنتج `ParserError`. شُخّص وأعيد بقائمة ملفات صريحة، ونجح؛ لا أثر على المطابقة.

## تحقق معايير القبول

- [x] حُصرت مصادر التدقيق والحوكمة والاختبارات السابقة ذات الصلة.
- [x] طوبقت الملاحظات الجوهرية بالكود/الإعداد الحالي.
- [x] صُنفت إلى تم إصلاحها/ما زالت/تدهورت/غير قابلة للتحقق.
- [x] الحالات المقصودة غير المكتملة مفصولة عن العيوب.
- [x] وُضعت قاعدة منع تكرار النتائج في سجل المخاطر النهائي.
