# ADR-UI-006 — زر مسح التقويم المركزي وسقف عناصره الأصلية

**الحالة:** Accepted
**التاريخ:** 2026-09-04
**المالك:** UI Platform
**السجل المتأثر:** `baseer-date-picker` / `UI_NATIVE_CONTROL_RATCHET.json`
**النطاق:** `apps/web/src/baseer-calendar-picker.tsx`

## السياق

يتضمن عقد `BaseerDatePicker` حالة `clearable` المسجلة صراحةً. تنفيذها المركزي
يحتاج زر مسح مستقل قابل للوصول داخل حقل التاريخ، إضافة إلى زر الفتح، زر الإغلاق،
أزرار التنقل، شبكة الأيام أو الشهور، وزر اليوم. سجل ratchet السابق بقي عند سبعة
أزرار خام رغم أن تنفيذ العقد الملتزم يحتوي ثمانية أنماط JSX من عناصر `button`.

## القرار

يعتمد سقف `baseer-calendar-picker.tsx` بثمانية أزرار فقط (`[8, 0, 1, 0]`). هذه
الأزرار جزء من primitive مركزي مسجل وليست عناصر ميزة محلية. يبقى أي ارتفاع لاحق
مرفوضاً، ويظل عقد `clearable` وISO والحدود وReact Hook Form كما هو.

## بدائل مرفوضة

| البديل | سبب الرفض | متى يعاد تقييمه |
| --- | --- | --- |
| حذف زر المسح | يكسر حالة `clearable` المعتمدة ويجبر المستخدم على تعديل يدوي. | لا يعاد إلا إذا أزيلت الحالة من العقد بالكامل. |
| استخدام زر صفحة أو ميزة محلية | ينقل سلوك primitive مركزي إلى واجهات المستهلكين ويعيد التكرار. | لا ينطبق. |
| رفع السقف بلا ADR | يتجاوز سياسة ratchet ولا يترك سبباً قابلاً للمراجعة. | لا ينطبق. |

## أثر الوصول والأداء

- AR/EN وRTL/LTR: تسمية المسح مترجمة ومنفصلة عن قيمة ISO ذات اتجاه LTR.
- لوحة المفاتيح/التركيز/ARIA: الزر `type=button` و`aria-label`، ولا يرسل النموذج.
- الجوال: يبقى زر مسح مستقل داخل الحقل ولا يتداخل مع زر التقويم.
- أثر bundle ونتيجة القياس: لا حزمة أو طلب شبكة أو وزن إنتاجي جديد.
- server authority أو security: لا API أو بيانات أو صلاحيات أو حسابات متأثرة.

## خطة التحقق والرجوع

- الاختبارات: `npm run check:ui-native-control-ratchet` ثم `quality` و`web-acceptance` في Linux.
- السجل: تحديث سقف ratchet وسجل المكوّن وفهرس القرار.
- التراجع: يعكس هذا القرار والسقف إلى سبعة فقط إذا أزيل زر المسح من العقد والتطبيق معاً.

## الاعتماد

| المراجع | القرار | التاريخ |
| --- | --- | --- |
| UI Platform owner | Accepted for the registered central primitive | 2026-09-04 |
| Engineering owner | Accepted after Linux quality finding | 2026-09-04 |
