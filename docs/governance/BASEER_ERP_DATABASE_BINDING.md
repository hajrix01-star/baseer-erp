# قاعدة بيانات الموقع المعتمدة — Baseer ERP

**آخر مراجعة:** 28 أغسطس 2026  
**الحالة:** نقطة الربط المعتمدة للموقع المحلي

## القاعدة المعتمدة

| الحقل | القيمة |
| --- | --- |
| الحاوية | `baseer-erp-postgres` |
| المضيف المحلي | `127.0.0.1:5433` |
| قاعدة البيانات | `baseer_erp_test` |
| النطاق المقصود | `baseer-viewer` — Baseer local viewer |
| الشركة النشطة | مشويات المعلم الشامي |
| معرّف الشركة | `4af6969a-161f-4e13-8acc-103d8aa26a70` |
| المنطقة الزمنية | `Asia/Riyadh` |

لا تحفظ كلمات المرور أو رابط الاتصال الكامل في هذا المستند. مصدر إعداد التطوير المحلي هو `apps/api/.env.baseer-test`.

## لا تستخدم للموقع

- `baseer_migration_staging`: بيئة مراجعة ترحيل وليست بيانات تشغيل الشركة.
- `baseer_local_postgres` / قاعدة `baseer`: مشروع منفصل لـARZ Lounge، وليس Baseer ERP.
- `nurix-rehearsal-20260827`: بيئة ترحيل/تجارب منفصلة.
- `postgres`: قاعدة إدارية بلا بيانات أعمال.

## ضوابط الربط والتحقق

1. يعمل API المحلي بقاعدة `baseer_erp_test` فقط.
2. تحدد الجلسة نطاق `baseer-viewer`؛ قد يجعل RLS القاعدة تبدو فارغة عند غياب هذا السياق.
3. لا تستخدم قاعدة الترحيل ولا تستبدل قاعدة الاختبار دون نسخة معزولة.
4. بعد التشغيل: يسجل المالك الدخول إلى «مشويات المعلم الشامي»، ويظهر موظف الشركة النشط فقط، ولا تظهر شركات ARZ أو الترحيل.
