# بطاقة الأثر — تسوية حارس عناصر الواجهة الخام

- **المعرف:** `BASEER-IMPACT-2026-09-03-UI-NATIVE-CONTROL-RATCHET-RECONCILIATION`
- **التصنيف:** `CONTROLLED` تحت `BASEER-ARCH v1.0`؛ لا يتغير مكوّن أو رحلة، لكنه يضبط حارس نظام التصميم المشترك.
- **الهدف:** تسجيل الأسقف الفعلية للمصادر الموجودة في المرشح والتي كانت غائبة من `UI_NATIVE_CONTROL_RATCHET.json` أو تجاوزت سقفًا قديمًا، كي يعود الحارس إلى رفض أي زيادة مستقبلية بدلاً من تعطيل CI بسبب خط أساس متقادم.
- **النطاق:** سجل `UI_NATIVE_CONTROL_RATCHET.json` وسجل الحوكمة فقط.
- **خارج النطاق:** لا JSX/CSS أو API أو بيانات أو مكتبة أو تغيير مرئي.

## G0–G4

- **G0:** الجرد الثابت عدّ عناصر `button/select/input/textarea` لكل مصدر فاشل في CI. معيار القبول: يمر الحارس بعد مطابقة السقف مع المصدر الحالي، ويرفض أي زيادة لاحقة في المصدر نفسه.
- **G1:** فحص static فقط؛ لا سعة أو قاعدة بيانات أو استخدام مستخدم.
- **G2:** لا عقد أو حقيقة أعمال أو صلاحية. السجل لا يمنح استثناءً مفتوحًا؛ كل قيمة هي ceiling، والانخفاض فقط مسموح بلا مراجعة جديدة.
- **G3/G4:** لا اعتماد أو واجهة جديدة. سجل الأسقف يشمل المكونات المركزية والمصادر التاريخية القائمة كما هي، ولا يصرح بمكوّن محلي جديد.

## التحقق والتراجع

- **التحقق المطلوب:** `npm run check:ui-native-control-ratchet` ثم CI الكامل؛ يظل كل ارتفاع لاحق فاشلاً حتى يراجع ببطاقة أثر/ADR مستقلة.
- **التراجع:** عكس commit التسوية. لا أثر تشغيلي أو بيانات أو نشر.
