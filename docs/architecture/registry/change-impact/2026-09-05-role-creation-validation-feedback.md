# أثر التغيير — توضيح التحقق عند إنشاء الدور

- **المرجع:** `BASEER-ARCH v1.0`، `lastVerifiedCommit: 4866d2f9`.
- **التصنيف:** `ARCHITECTURAL` لأن الرحلة تقع داخل هوية وإدارة المنصة والصلاحيات، رغم أن التعديل واجهة فقط.
- **النطاق:** `apps/web/src/administration-roles-panel.tsx` وطبقة التحقق العامة للواجهة، والتحقق التكاملـي لمسار `POST /administration/roles`. لا يتغير كتالوج الصلاحيات أو تفويض الخادم أو بنية Prisma.
- **العقد الثابت:** يظل الخادم المالك للتحقق من المالك، وعزل المستأجر، ومعرفة مفاتيح الصلاحيات؛ تظل قاعدة الدور: رمز كبير صالح، اسمان، وصلاحية واحدة على الأقل.
- **المشكلة المثبتة:** عند خطأ إدخال، يعرض النموذج رسالة عامة في نهاية الرحلة ولا يمكن لـReact Hook Form نقل التركيز إلى الحقول المتحكم بها غير المسجلة؛ على الجوال تبدو عملية الإضافة معطلة.
- **المعالجة والاختبار:** إظهار الأخطاء الحقلية الموجودة، وإرسال المستخدم لأول خطأ ظاهر في الرحلة، مع تحقق TypeScript للويب. لا تغير للبيانات، لذلك لا يلزم مسار توافق أو ترحيل.
- **المتابعة المثبتة:** لقطة ما بعد الإصدار تحتوي على رمز واسمين وصلاحيتين، لكنها تتلقى خطأ تنفيذ من الخادم؛ لذلك يضاف اختبار تكاملي لإنشاء الدور والتحقق من منح الصلاحيات قبل أي تعديل سلوكي للخادم.
- **قرار العقد (2026-09-05):** رمز الدور معرّف داخلي لا يُدخل في واجهة الإدارة. يبقى الحقل اختيارياً لفترة توافق قصيرة مع العملاء السابقين، لكن الخادم يولده من الاسم الإنجليزي مع لاحقة UUID قصيرة؛ لا يؤخذ الرمز المرسل من العميل كمصدر حقيقة. لا يلزم ترحيل لأن الرموز الحالية لا تتغير.
- **الأثر:** `packages/contracts/src/administration.ts`، خدمة الإدارة، عميل ولوحة الويب، واختبار دورة الإدارة. المخاطر المحكومة: التكرار والتزامن؛ المعرّف القصير يجعل الرمز فريداً عملياً داخل القيد الفريد للمستأجر، ويظل القيد في قاعدة البيانات حارساً نهائياً.
