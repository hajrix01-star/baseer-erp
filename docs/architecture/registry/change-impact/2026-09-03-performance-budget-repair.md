# بطاقة الأثر — إصلاح بوابة أداء الواجهة

- **المعرف:** `BASEER-IMPACT-2026-09-03-PERFORMANCE-BUDGET`
- **التصنيف:** `CONTROLLED` تحت `BASEER-ARCH v1.0`.
- **الهدف:** إعادة تفعيل بوابة حجم الواجهة التي تمنع انحدار سرعة التحميل بعد أن تغيّر اسم مخرج مكوّن التاريخ العام في Vite.
- **النطاق:** `scripts/check-web-release-budget.mjs` فقط، وسجل الحوكمة.
- **خارج النطاق:** لا تعديل لواجهة المستخدم أو API أو قاعدة البيانات أو بيانات ERP أو الصلاحيات أو المكتبات.
- **G0/G1:** القياس السابق لنسخة الإنتاج المحلية أعطى LCP جوال 2.1s وحجم شاشة بداية 169KiB. بوابة الحجم تقيس ملفات البناء المضغوطة؛ حدود الرحلات الحالية تبقى نافذة ولا تمثل وعداً بإنتاج أو بيانات كبيرة.
- **G2:** مصدر الأرقام هو manifest وملفات `apps/web/dist` الناتجة من بناء حديث. لا توجد أرقام أعمال أو حسابات عميل.
- **G3:** لا اعتماد جديد؛ المسار المباشر هو تحديث الحارس من مصدر داخلي لا يظهر منفصلاً في manifest إلى مدخل Vite المسمّى للمكوّن العام الذي ينتجه فعلاً.
- **G4:** لا تغير مرئي أو حركة أو لغة.
- **معيار القبول:** بعد بناء الويب، ينجح `npm run verify:web-budget` ويستمر في رفض زيادة runtime التقويم فوق حد التفاعل الموثق.
- **الدليل:** بناء Vite حديث ثم `npm run verify:web-budget` نجح: بداية 79,519B/251,000B gzip، أكبر رحلة 59,505B/95,000B، مكوّن التاريخ 6,296B/200,000B، الرسم البياني المؤجل 179,684B/500,000B، وشبكة البيانات 10,384B/50,000B.
- **التراجع:** تراجع commit هذا التغيير؛ لا أثر بيانات أو نشر.
- **حالة التنفيذ:** G0–G7 مقفلة محلياً. هذا يعيد حارس الانحدار؛ ولا يدعي قياس الإدخال أو التقارير على بيانات إنتاج.
