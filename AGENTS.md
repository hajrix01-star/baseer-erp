# التوجيه المعماري التلقائي

كل طلب بناء أو تعديل أو إصلاح أو مراجعة تسليم داخل هذا المشروع يمر تلقائياً عبر `$alpha-architects-team` قبل التنفيذ أو الحكم. لا يطلب المستخدم استدعاء الفريق.

- مدير المشروع يقرأ `docs/architecture/registry/INDEX.md` ويصنّف الأثر: `LOCAL` أو `CONTROLLED` أو `ARCHITECTURAL`.
- `LOCAL`: يستعمل المرجع القائم ويسجل الأثر المختصر؛ لا يعيد استكشاف الخريطة ولا ينتظر موافقة بشرية.
- `CONTROLLED`: يحصل فريق البناء على خريطة أثر مصغرة قبل التغيير؛ يحدّث المرجع قبل التسليم.
- `ARCHITECTURAL` أو مرجع مفقود/قديم: يوقف البناء حتى يحدّث فريق المعماريين المرجع والقرار/العقد اللازمين.
- فريق ألفا للبناء يملك التنفيذ والاختبارات. فريق تسليم ألفا يراجع نسخة المرجع وسجل الأثر نفسيهما ولا يرسم خريطة من الصفر.
- لا تسري هذه القاعدة على الأسئلة المعرفية أو القراءة البحتة التي لا تطلب تغييراً أو قبول إصدار.

# التنسيق الذكي للمهارات والأجنتات

عندما يحتاج العمل إلى أكثر من أمر/قراءة، أو بحث، أو تفويض أجنتات، يطبق مدير المشروع `$alpha-efficient-orchestration` تلقائياً قبل التوسع. يعيد استخدام registry ونتائج الجلسة وحالة الأجنتات أولاً، ثم يختار أقل مهارات وفرق وأوامر مستقلة تكفي للدليل.

- لا يحمل كل المهارات أو يعيد مسح الخريطة لمجرد توفرها؛ يعلن القدرات المختارة والسبب فقط عند وجود عمل متعدد الخطوات.
- لا يرسل أجنتين للسؤال أو الملفات نفسها؛ لكل أجنت نطاق ومالك مخرجات محددان، ويعاد استخدام أجنت قائم أو نتيجة حديثة قبل إنشاء آخر.
- يبدأ بفحص محلي ضيق ونتيجة موجودة، ثم يوسع إلى اختبار أو شبكة فقط إذا تغيرت البصمة أو كان الدليل غير كافٍ.
- لا يعيد أمراً أو بحثاً أو اختباراً لم يتغير نطاقه/إصداره، ولا يحول السرعة إلى تجاوز فحص مالي أو أمني أو تسليم مطلوب.
